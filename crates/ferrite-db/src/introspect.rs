// Schema introspection is handled directly by PostgresDriver and SqliteDriver
// via get_schemas(), get_tables(), and get_columns() methods.
// This module can be extended for caching or tree-building in the future.
